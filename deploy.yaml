---
- hosts: all
  gather_facts: true

- hosts: all:!localhost
  gather_facts: false
  roles:
    - lasair_instance

- hosts: kafka
  gather_facts: false
  roles:
    - kafka

# we currently run mirrormaker on the same hosts as kafka/zookeeper 
- hosts: kafka
  gather_facts: false
  roles:
    - mirrormaker
 
